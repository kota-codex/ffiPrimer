# ffiPrimer
An example of the Argentum module that combines ag and C/C++ code and vcpkg dependencies. It builds module using github action on all platforms and packages it to be used in ag-package manager. 
